```
在题目中出现最大、最小、最长、存在等字眼的时候，可以考虑使用动态规划的思路。逐步推导出最大值。
这种最大、最长、都是可以由前面推导出后面的值，满足条件就+1，不满足就是之前的值

根据题目中是否有：计数、最大/最小/最长、是否存在等字眼，先判断是否可以使用动态规划解决，如果可以，然后根据上面的步骤，一步一步进行分析，尤其是最后一步这一步分析，是能否转化为子问题的关键。转化为子问题后，就能轻易得到转移方程，后面的操作就简单了。

有些题目，动态规划不一定是最高效的解法，但是根据这个套路进行分析，一定是最快的解法。先写出来之后再考虑是否可以优化，或者其他更优的解法。
```

```
string 中的spilt方法用于切割字符串，如果全部为待切割的值，则返回空数组，如果包含其他的值时，待切割的值用“”代替。
注：超过两个连续的时候（不包含两个），此时空格的个数为字母的次数-1

join是很好的拼接函数
```

```
在一组数据中求值可以借助map 来获取对应关系
```

