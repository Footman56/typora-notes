结构为：

1. 展示层： 负责向用户展示信息和解释用户命令，完成前端页面逻辑，并将用户请求传递给应用层
2. 应用层：负责协调领域层的领域对象，组成具体应用场景。应用层直接依赖领域层，由领域层提供具体业务能力，不包含业务规则或者只是
3. 领域层：包含业务的领域对象、领域服务、他们之间的关系。负责表达业务概念，不需要外部依赖，只是反应软件核心业务
4. 基础设施层：向其他层提供通用能力，为应用层传递消息，为领域层提供持久化机制
5.  