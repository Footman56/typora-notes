高并发模型是 漏斗型

1. 客户端/CND拦截无效流量，做静态资源缓存。
2. 网关层：识别恶意流量，进行全局限流。
3. 缓存层：抗住核心并发，做库存预扣。
4. 数据库层：只负责最终的数据持久化，不再承担计算压力。



# 客户端

1. 静态资源加速 ，存入阿里云的CDN，

2. 按钮防抖 与URL 动态化

   ​	动态 URL：为了防止黑客通过脚本提前探测接口，秒杀接口的 URL 不能写死。通常的做法是，秒杀开始前由服务器下发一个随机的Salt（盐值），前端通过算法生成动态 URL（如/seckill/verify_hash_x9zz ），服务器校验通过才放行。

# 网关层

通常有nginx  配合LUA脚本实现

1. WAF 流量清洗： 会有大量的“羊毛党”使用脚本进行机器刷单。技术实现：网关层接入WAF（Web应用防火墙），基于IP 频率、
   User-Agent 特征甚至机器学习模型进行识别。例如，一个IP 在1秒内发起了500次请求，或者 HTTP 头缺失关键字段，直接返回403 Forbidden，将这些非人类流量拒之门外。

2. 全局限流算法

   采用常见的限流策略：有令牌桶和漏桶

   ##令牌桶 +redis 

   - 以固定速率往桶里放令牌（比如每秒 100 个）
   - 桶有最大容量（比如 1000）
   - 请求来了必须先拿一个令牌
   - 没有令牌 → 拒绝请求

   允许突发流量、控制长期平均速率

​		##漏桶

   + 
     + 请求不断往桶里倒水
     + 水以固定速率流出（比如每秒 10 个）
     + 桶满了 → 新请求丢弃

不允许突发

# redis + lua 脚本

# MQ蓄水池

在 Redis 中抢到库存，并不代表订单真正创建成功。如果此时直接调用数据库写订单，脆弱的 MySQL 依然会被瞬间击垮。
图灵课堂
异步解耦与削峰填谷 我们引入 消息队列（RabbitMQ / RocketMQ / Kafka） 作为中间件：

1. 异步处理：Redlis 扣减库存成功后，立即向 MQ发送一条“创建订单”的消息，并直接给前端返回“排队中”的状态。用户不需要等待订单真正落库，体验极佳。
2. 削峰：MQ 就像一个巨大的蓄水池，它能暂存上游 Redis 涌入的成干上万条消息。
3. 填谷：下游的订单服务（Consumer））根据自己的处理能力（比如每秒处理500单），匀速地从MQ 中拉取消息并写入数据库。
   通过这种方式，我们将“脉冲式”的流量冲击，转化为了“流式”的平稳处理。

# 数据库

乐关锁 来保证数据一致性