# 优化顺序

不要一上来就分库分表。

正确顺序：

1. 建好索引
2. SQL 优化
3. 读写分离
4. 垂直拆分
5. 水平分表
6. 水平分库



# 读写分离

# 垂直拆分

# 分库分表





# 解决跨库join

##  应用层整合

分次查询，内存聚合

## 数据冗余

在表中冗余频繁关联的字段，转成单库操作。

会有数据一致性问题？

用户在更新完数据库后，向MQ发送一条 已变更的消息、监听消息后 所有表都变更

## 中间件

借助中间件作为代理层，应用层仍编写标准sql ，由中间件去实现。

