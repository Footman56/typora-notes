抽象类是 abstract定义的类，可以有实例字段、构造函数、静态方法、静态字段、抽象方法、非抽象方法。单继承。可以有 `private`、`protected`、`public` 等方法。

接口 中有抽象方法、default 方法 、static方法，不能有构造方法。多实现。Java 8 前只能是 `public`，Java 9 后允许 `private` 方法（通常用于 default 方法的代码复用）