```
Failed to auto-configure a DataSource: 'spring.datasource.url' is not specified and no embedded datasource could be auto-configured.

如果你需要用到数据库组件，那么就需要配置一下数据源相关的信息；如果暂时不需要数据库组件，那么可以在项目启动类上，这样写@SpringBootApplication(exclude = {DataSourceAutoConfiguration.class})就可以了；或者就是注释掉pom文件中用到的数据库相关starter组件引入，重新更新一下maven项目依赖也可以。
```



jar 运行时提示没有 主清单属性

![image-20241021004040586](https://gcore.jsdelivr.net/gh/Footman56/imageBeds/202410210040790.png)

```xml
 <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <configuration>
                    <mainClass>com.huochai.jsonchange.JsonChangeApplication</mainClass>
                    <skip>false</skip>
                </configuration>
                <executions>
                    <execution>
                        <id>repackage</id>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
```

<skip>false</skip> 要设置false









项目中突然间所有jar失效，代码中无法获取对应的类。

解决方式：

<img src="https://raw.githubusercontent.com/Footman56/images/master/img202502171718780.png" alt="image-20250217171844110" style="zoom:50%;" />
