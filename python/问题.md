# 使用pip 下载问题

<img src="https://gcore.jsdelivr.net/gh/Footman56/imageBeds/202404281518632.png" alt="image-20240428151842246" style="zoom:50%;" />

1. **使用Homebrew安装**: 如果您要安装的包是可以通过Homebrew安装的，那么可以使用Homebrew来安装。例如，如果需要安装`numpy`，可以使用命令`brew install numpy`。

2. **创建虚拟环境**: 如果您需要安装的包不在Homebrew中，建议创建一个虚拟环境，然后在虚拟环境中使用pip安装所需的包。这是最推荐的方法，因为它可以隔离您的项目依赖，避免影响系统级的Python环境。

   创建虚拟环境的命令如下：

   ```
   python3 -m venv /usr/local/opt/python@3.12/libexec/bin/venv
   source /usr/local/opt/python@3.12/libexec/bin/venv/bin/activate
   python3 -m pip install package-name
   ```

   其中`path/to/venv`是您希望创建虚拟环境的路径，`package-name`是您想要安装的包名。

3. **使用pipx**: 如果您是在安装一个Python应用程序，pipx是一个好的选择，因为它可以为您管理的包创建隔离的虚拟环境。

   安装pipx的命令如下：

   ```
   brew install pipx
   ```

   然后使用pipx安装应用程序：

   ```
   pipx install package-name
   ```

4. **修改pip行为**: 如果您坚持要直接使用pip安装到系统Python目录，可以通过添加`--break-system-packages`标志给pip，或者在pip配置文件中添加`break-system-packages = true`来修改pip的行为。但请注意，这样做可能会破坏Homebrew管理的Python环境，因此**强烈不推荐**。

   如果您选择这种方法，最好同时使用`--user`标志，这样至少会将包安装到用户目录下，而不是系统Python目录。

请根据您的具体情况选择最适合的方法。通常来说，创建虚拟环境是最安全、最干净的方法

选择步骤4 

1. 查看pip的配置文件
pip -m config list

![image-20240428160653153](https://gcore.jsdelivr.net/gh/Footman56/imageBeds/202404281606226.png)

2. 编辑pip.conf

   ```
   [global]
   # 配置代理地址
   index-url = https://pypi.tuna.tsinghua.edu.cn/simple
   # 安装系统python目录
   break-system-packages = true
   ```

3.  查看下载包路径

   ```
   python3 -m site
   ```

   <img src="https://gcore.jsdelivr.net/gh/Footman56/imageBeds/202404281609589.png" alt="image-20240428160903547" style="zoom:50%;" />

   

后续就可以直接使用pip 下载啦

# 下载出错

<img src="https://gcore.jsdelivr.net/gh/Footman56/imageBeds/202406032205911.png" alt="image-20240603220537152" style="zoom:50%;" />

python3.12 不兼容 pyyaml 6.0 

升级到 6.0.1 就可以





# 运行虚拟环节脚本

我们已经创建了虚拟环境，在当前目录下是可以的直接执行python  xx.py 命令的

当时想要在shell 中执行的话：

1. 需要先激活虚拟环境

2. 在执行python xx .py 

   ```sh
   #!/bin/bash
   
   # Required parameters:
   # @raycast.schemaVersion 1
   # @raycast.title 变量命名
   # @raycast.mode compact
   
   # Optional parameters:
   # @raycast.icon 🤖
   # @raycast.argument1 { "type": "text", "placeholder": "1-常量;2-变量;3-类名 4-包名" }
   # @raycast.argument2 { "type": "text", "placeholder": "变量" }
   
   # Documentation:
   # @raycast.author peilizhi450
   # @raycast.authorURL https://raycast.com/peilizhi450
   
   source   /Users/peilizhi/pythonProjects/test/.venv/bin/activate
   
   echo $(python3 /Users/peilizhi/pythonProjects/test/word.py $1 $2)
   ```

   



# 在pycharm 中使用虚拟环境

1. 先创建虚拟环境

   ```
   # myvenv 是虚拟环境的名称
   python3 -m venv myvenv
   ```

2.  激活虚拟环境

   ```
   source myvenv/bin/active
   ```

3. 激活成功后 终端会显示虚拟环境的名称

   <img src="https://gcore.jsdelivr.net/gh/Footman56/imageBeds/202407051059483.png" alt="image-20240705105940188" style="zoom:50%;" />

4. 在pycharm 中选择虚拟环境

   <img src="https://gcore.jsdelivr.net/gh/Footman56/imageBeds/202407051100455.png" alt="image-20240705110033406" style="zoom:50%;" />

5. 选择虚拟环境中的python 

   <img src="https://gcore.jsdelivr.net/gh/Footman56/imageBeds/202407051101423.png" alt="image-20240705110137372" style="zoom:50%;" />

6. 下载包

   ```sh
   python3 -m  pip install  matplotlib        
   ```

   
