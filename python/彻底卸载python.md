# wget 安装的

1. **确定 Python 安装路径**:

   通常，使用 `wget` 安装的 Python 会安装在 `/usr/local/bin` 或者 `/usr/local/opt` 路径下。你可以通过以下命令查找 Python 的安装路径

   ```
   which python
   ```

   

2. 卸载

   ```
   sudo rm -rf pip*
   ```

   





