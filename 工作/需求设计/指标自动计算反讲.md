```
目标确认、自评的时候，单个操作目标值（只在详情中修改）

目标登记的时候可以批量录入
目标确认、自评时根据详情进行更改。
```



子杰、

```
统一固定字段表和自定义字段表
洗数据的时候 把之前的分组字段洗成固定字段（分组字段中的自定义字段），不丢失字段【断服洗数据】
洗数据的时候根据公司来洗，记录每一个公司的洗数据情况，应洗多少条，实际洗成果多少条。
指标锁定说法需要更改，
指标锁定的时候，目标值、完成值无法修改-》指标库中不锁定目标值

引用指标库的指标时，只影响当前指标的锁定状态，不影响其他指标的表头，管理员不可以修改锁定的指标 =》 分组设置是通用设置，指标上是自定义设置。
分组字段控制放在指标上（后端处理逻辑，前端不改指标增加），引用指标库的指标 与考核表中不引用的 锁定状态不一样。
```

详情改造、

表单改造（指标分为：权重、指标内容），每一个指标内容可以不一样。引用的时候就将指标库中的所有覆盖到指标内容上。

指标库改造（锁定相关，指标库中字段是否锁定）

添加指标有两种路径，考核表和指标库



立志：

去掉方案管理员填写录入逻辑

流程图缺少完成值录入环节

是否在管理端新增已录入数据修改完成值数值

流程信息还是采用串型逻辑，完成值录入依旧存在流程信息中



彭杰：

自评环节 字段锁定 就不能批量导入

只有目标登记才可以批量导入逻辑入口，详情修改是目标什么时候能修改， 详情就能修改



